import React from "react";

const IndexFeedToggle = () => {
    return (
        <button className="index-feed-toggle">
            <span className="index-label">Index</span>
            <span className="feed-label">Feed</span>
        </button>
    );
};

export default IndexFeedToggle;
